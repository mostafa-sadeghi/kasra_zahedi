import tkinter as tk
from tkinter import ttk
from PIL import Image, ImageTk
root = tk.Tk()
# root.rowconfigure(0, weight=1)
# root.columnconfigure(0, weight=1)

# main_frame = ttk.Frame(root)
# main_frame.grid(column=0, row=0, sticky="n")
# main_frame.rowconfigure(0, weight=1, minsize=180)


# tk.Label(main_frame, text="Number1").grid(column=0, row=0)
# ttk.Separator(main_frame, orient="vertical").grid(
#     column=1, row=0, sticky="nsew")
# tk.Label(main_frame, text="Number2").grid(column=2, row=0)

# def print_current_selection():
#     print()

# image = Image.open("orangee.png")
# tk_image = ImageTk.PhotoImage(image)

# check_button = ttk.Checkbutton(
#     root, text="Pizza", image=tk_image, compound="right", command=lambda: print("hello"))
# check_button.pack()
# check_button["state"] = "disabled"  # "normal"
# root.mainloop()
